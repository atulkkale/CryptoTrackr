import SectionWrapper from "../components/SectionWrapper/SectionWrapper";

const History: React.FC = () => {
  return <SectionWrapper sectionClass="dashboard">History</SectionWrapper>;
};

export default History;
